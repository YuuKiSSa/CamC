package org.example.ad.repository;

import org.example.ad.model.CameraWebsite;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CameraWebsiteRepository extends JpaRepository<CameraWebsite, Long> {
}